[project]
name = "masteringpytorch"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "modal>=1.5.1",
    "torch>=2.13.0",
    "torchvision>=0.28.0",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
